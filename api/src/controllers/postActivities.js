// const { Activity } = require('../db');

// const newActivities = []
// const postActivity = async (name, difficulty, duration, season) => await Activity.create({
//     name: name,
//     difficulty: difficulty,
//     duration: duration,
//     season: season
// });

// const NewActivity = await postActivity("name", "difficulty", "duration", "season");

// newActivities.push(NewActivity);


// module.exports = postActivity;

// //! CREE UNA ARRAY VACIA DONDE SE VAN A PUSHEAR LAS NUEVAS ACTIVIDADES para el post.
